we will build a fitness app that uses computer vision to analyze form during lifting. we will use openrouter for the api keys. supabase for persistence. stack-auth for authentication. as additional features, we can have a separate ai agent develop fitness and diet plans. we need a modern UI. this will be primarily image/video/vision based app where users confirm. for instance, users will upload or take a photo of a meal. the model will identify and record the meal. user only needs to confirm. onboarding follows the same minimal friction process. user snaps a photo (or uploads) and model identifies approximate age, gender, and bmi and suggests fitness plan. user can confirm. for advanced users, they can select diet type (eg keto, vegan, mediter etc...) and add calorie targets. the same process for lifting. automatically identify lifting routine but give options to select from like the mike israetel or greg doucette philos (or add their own custom approach that the model then builds workouts for). we don't want dramull/copilottest · Started 3 minutes ago · Queued Copilot we will build a fitness app that uses computer vision to analyze form during lifting. we will use openrouter for the api keys. supabase for persistence. stack-auth for authentication. as additional features, we can have a separate ai agent develop fitness and diet plans. we need a modern UI. this will be primarily image/video/vision based app where users confirm. for instance, users will upload or take a photo of a meal. the model will identify and record the meal. user only needs to confirm. onboarding follows the same minimal friction process. user snaps a photo (or uploads) and model identifies approximate age, gender, and bmi and suggests fitness plan. user can confirm. for advanced users, they can select diet type (eg keto, vegan, mediter etc...) and add calorie targets. the same process for lifting. automatically identify lifting routine but give options to select from like the mike israetel or greg doucette philos (or add their own custom approach that the model then builds workouts for). we don't want dramull/copilottest · Started 4 minutes ago · Queued Copilot we will build a fitness app that uses computer vision to analyze form during lifting. we will use openrouter for the api keys. supabase for persistence. stack-auth for authentication. as additional features, we can have a separate ai agent develop fitness and diet plans. we need a modern UI. this will be primarily image/video/vision based app where users confirm. for instance, users will upload or take a photo of a meal. the model will identify and record the meal. user only needs to confirm. onboarding follows the same minimal friction process. user snaps a photo (or uploads) and model identifies approximate age, gender, and bmi and suggests fitness plan. user can confirm. for advanced users, they can select diet type (eg keto, vegan, mediter etc...) and add calorie targets. the same process for lifting. automatically identify lifting routine but give options to select from like the mike israetel or greg doucette philos (or add their own custom approach that the model then builds workouts for). we don't want complex code that does simple things. we wat simple code that does compelx things. 

think not about what has been done but should or can be done for fitness apps. add the missing features i overlooked. deliver maximal ux for 2026 in a minimalist design that makes apple blush. 
